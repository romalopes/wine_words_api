<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= content_for(:title) || "Wine Words" %></title>
    <%= csrf_meta_tags %>
    <link rel="icon" type="image/png" href="/wine_words_icon.png">
    <style>
      :root { color: #281919; background: #faf7f2; font-family: Georgia, "Times New Roman", serif; }
      * { box-sizing: border-box; }
      body { margin: 0; }
      a { color: inherit; }
      .site-header { background: #4a1020; color: #fff8f0; padding: 1.25rem max(1.5rem, calc((100vw - 72rem) / 2)); }
      .site-header a { text-decoration: none; }
      .site-header__logo { display: flex; align-items: center; gap: .75rem; }
      .site-header__logo img { height: 2.5rem; border-radius: .35rem; display: block; }
      .site-header__title { font-size: 1.45rem; font-weight: 700; letter-spacing: .02em; }
      .site-header__subtitle { margin: .25rem 0 0; color: #ead7d1; font: 0.9rem system-ui, sans-serif; }
      main { max-width: 72rem; margin: 0 auto; padding: 2.5rem 1.5rem 4rem; }
      h1, h2, p { margin-top: 0; }
      h1 { color: #4a1020; font-size: clamp(2rem, 5vw, 3.25rem); margin-bottom: .5rem; }
      .lede { color: #6b5855; font: 1.05rem/1.6 system-ui, sans-serif; margin-bottom: 2rem; }
      .wine-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; }
      @media (max-width: 1200px) { .wine-grid { grid-template-columns: repeat(3, 1fr); } }
      @media (max-width: 900px) { .wine-grid { grid-template-columns: repeat(2, 1fr); } }
      @media (max-width: 600px) { .wine-grid { grid-template-columns: 1fr; } }
      .review-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; }
      @media (max-width: 1200px) { .review-grid { grid-template-columns: repeat(3, 1fr); } }
      @media (max-width: 900px) { .review-grid { grid-template-columns: repeat(2, 1fr); } }
      @media (max-width: 600px) { .review-grid { grid-template-columns: 1fr; } }
      .producer-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; }
      @media (max-width: 1200px) { .producer-grid { grid-template-columns: repeat(3, 1fr); } }
      @media (max-width: 900px) { .producer-grid { grid-template-columns: repeat(2, 1fr); } }
      @media (max-width: 600px) { .producer-grid { grid-template-columns: 1fr; } }
      .producer-card { display: block; background: #fff; border: 1px solid #eaded7; border-radius: .7rem; box-shadow: 0 2px 10px rgba(63, 24, 24, .06); overflow: hidden; text-decoration: none; transition: transform .15s, box-shadow .15s; }
      .producer-card:hover { transform: translateY(-3px); box-shadow: 0 7px 18px rgba(63, 24, 24, .14); }
      .producer-card__band { height: .5rem; background: #6b5855; }
      .review-card { display: block; background: #fff; border: 1px solid #eaded7; border-radius: .7rem; box-shadow: 0 2px 10px rgba(63, 24, 24, .06); overflow: hidden; text-decoration: none; transition: transform .15s, box-shadow .15s; }
      .review-card:hover { transform: translateY(-3px); box-shadow: 0 7px 18px rgba(63, 24, 24, .14); }
      .review-card__band { height: .5rem; background: #27615e; }
      .review-card__content { padding: 1.1rem; }
      .producer-card__content { padding: 1.1rem; }
      .group-title { font-size: 1.5rem; font-weight: 700; color: #4f4440; margin: 1.5rem 0 1rem 0; padding-bottom: 0.5rem; border-bottom: 2px solid #e4d9cd; }
      .image-gallery__item { position: relative; display: inline-block; margin: .25rem; }
      .image-gallery__remove { position: absolute; top: .25rem; right: .25rem; }
      .wine-card { display: block; background: #fff; border: 1px solid #eaded7; border-radius: .7rem; box-shadow: 0 2px 10px rgba(63, 24, 24, .06); overflow: hidden; text-decoration: none; transition: transform .15s, box-shadow .15s; }
      .wine-card:hover { transform: translateY(-3px); box-shadow: 0 7px 18px rgba(63, 24, 24, .14); }
      .wine-card__band { height: .5rem; background: #8a273c; }
      .wine-card--white .wine-card__band { background: #c49b38; }
      .wine-card--rose .wine-card__band, .wine-card--rosé .wine-card__band { background: #d47386; }
      .wine-card__content { padding: 1.1rem; }
      .wine-card h2 { font-size: 1.25rem; margin-bottom: .3rem; }
      .region, .details, .vintages, .back-link { color: #6b5855; font: .9rem/1.5 system-ui, sans-serif; }
      .details { margin: .7rem 0; }
      .tag { display: inline-block; background: #f3e9e3; border-radius: 99px; color: #6b2834; font: .78rem system-ui, sans-serif; margin: .15rem .25rem 0 0; padding: .22rem .5rem; }
      .wine-page { max-width: 48rem; }
      .back-link { display: inline-block; margin-bottom: 1.5rem; }
      .facts { display: flex; flex-wrap: wrap; gap: .75rem; list-style: none; margin: 1.5rem 0 2rem; padding: 0; }
      .facts li { background: #fff; border: 1px solid #eaded7; border-radius: .45rem; padding: .7rem .9rem; font: .9rem system-ui, sans-serif; }
      .prompt { border-left: 4px solid #8a273c; color: #513e3b; font-size: 1.1rem; line-height: 1.65; padding-left: 1rem; }
      .vintage-list { display: grid; gap: .8rem; padding: 0; list-style: none; }
      .vintage-list li { background: #fff; border: 1px solid #eaded7; border-radius: .5rem; padding: 1rem; }
      .vintage-list strong { color: #6b2834; }
      .image-gallery { display: flex; flex-wrap: wrap; gap: .75rem; margin-bottom: 1.5rem; }
      .image-gallery .gallery-img { width: 11rem; height: 8rem; object-fit: cover; border-radius: .5rem; border: 1px solid #eaded7; }
      .card-thumb { width: 100%; height: 8rem; object-fit: cover; display: block; }
      .empty { color: #6b5855; font: 1rem system-ui, sans-serif; }
      .site-nav { display: flex; flex-wrap: wrap; gap: 1rem; margin-top: .75rem; font: 1rem system-ui, sans-serif; }
      .site-nav a { color: #ead7d1; padding: .25rem 0; border-bottom: 2px solid transparent; }
      .site-nav a:hover, .site-nav a.active { color: #fff8f0; border-bottom-color: #c49b38; }
      .site-nav__auth { margin-left: auto; background: #8a273c; border-radius: 99px; color: #fff8f0; padding: .35rem 1rem; }
      .site-nav__user { color: #fff8f0; font-weight: 700; align-self: center; }
      .filter-pills { display: flex; flex-wrap: wrap; gap: .5rem; margin-bottom: .75rem; }
      .filter-pill { display: inline-block; border: 1px solid #d7c8bb; border-radius: 999px; padding: .35rem 1rem; font: 0.85rem system-ui, sans-serif; font-weight: 700; color: #4f4440; background: #fff; text-decoration: none; }
      .filter-pill:hover { border-color: #8a273c; color: #8a273c; }
      .filter-pill.active { background: #8a273c; border-color: #8a273c; color: #fff8f2; }
      .filter-pill--status.active { background: #27615e; border-color: #27615e; }
      .filter-pill--status { padding: .25rem .8rem; font-size: 0.78rem; }
      .site-nav__auth:hover { filter: brightness(1.15); }
      .flash { max-width: 72rem; margin: 1rem auto 0; padding: .75rem 1rem; border-radius: .5rem; font: .95rem system-ui, sans-serif; }
      .flash--notice { background: #e4f3e4; color: #245a24; }
      .flash--alert { background: #f9e3e3; color: #8a273c; }
      .auth-page { display: flex; justify-content: center; }
      .auth-card { width: min(28rem, 100%); background: #fff; border: 1px solid #eaded7; border-radius: .8rem; box-shadow: 0 4px 16px rgba(63,24,24,.08); padding: 2rem; }
      .wine-kicker { text-transform: uppercase; letter-spacing: .12em; color: #8a273c; font: .78rem system-ui, sans-serif; margin: 0 0 .5rem; }
      .auth-form { display: grid; gap: 1rem; margin: 1.5rem 0; }
      .auth-form__field { display: grid; gap: .35rem; font: .9rem system-ui, sans-serif; color: #513e3b; }
      .auth-form__field input { border: 1px solid #d8c8c0; border-radius: .45rem; padding: .6rem .75rem; font: 1rem system-ui, sans-serif; }
      .auth-form__submit { background: #8a273c; color: #fff8f0; border: none; border-radius: 99px; padding: .65rem 1rem; font: 600 1rem system-ui, sans-serif; cursor: pointer; }
      .auth-form__error { background: #f9e3e3; color: #8a273c; border-radius: .45rem; padding: .6rem .8rem; font: .9rem system-ui, sans-serif; }
      .auth-card__switch { font: .95rem system-ui, sans-serif; color: #6b5855; }
      .text-link { color: #8a273c; font-weight: 600; }
      .auth-hint { background: #f3e9e3; border-radius: .5rem; padding: .8rem 1rem; font: .95rem system-ui, sans-serif; }
      .page-actions { display: flex; justify-content: flex-end; margin-bottom: 1.25rem; }
      .page-actions form, .card-actions form, .vintage-list form { display: inline-block; margin: 0; }
      .btn { display: inline-block; background: #8a273c; color: #fff8f0; border: none; border-radius: 99px; padding: .45rem 1.1rem; font: 600 .9rem system-ui, sans-serif; text-decoration: none; cursor: pointer; }
      .btn:hover { filter: brightness(1.15); }
      .btn--secondary { background: #6b5855; }
      .site-nav__settings { position: relative; display: inline-flex; align-items: center; align-self: center; }
      .site-nav__settings summary { cursor: pointer; list-style: none; color: #ead7d1; padding: .25rem 0; border-bottom: 2px solid transparent; }
      .site-nav__settings summary:hover, .site-nav__settings[open] summary { color: #fff8f0; border-bottom-color: #c49b38; }
      .site-nav__settings summary::-webkit-details-marker { display: none; }
      .site-nav__settings-menu { position: absolute; top: calc(100% + .35rem); left: 0; background: #fff; border: 1px solid #d8c8c0; border-radius: .5rem; box-shadow: 0 8px 20px rgba(0,0,0,.15); display: flex; flex-direction: column; min-width: 10rem; padding: .35rem 0; z-index: 100; }
      .site-nav__settings-menu a { color: #6b2834; font: .9rem system-ui, sans-serif; padding: .45rem .9rem; white-space: nowrap; }
      .site-nav__settings-menu a:hover { background: #f3e9e3; }
      .category-flags { border: 1px solid #d8c8c0; border-radius: .5rem; display: flex; gap: 1.25rem; margin-bottom: 1rem; padding: .6rem .9rem; max-width: 30rem; }
      .category-flags legend { font: 600 .85rem system-ui, sans-serif; padding: 0 .4rem; }
      .category-flags label { display: flex; align-items: center; gap: .35rem; }
      .card-wrapper { position: relative; }
      .card-actions { position: absolute; top: .75rem; right: .75rem; z-index: 1; display: flex; gap: .4rem; align-items: center; }
      .card-action { background: #fff; border: 1px solid #d8c8c0; border-radius: 99px; color: #6b2834; font: .78rem system-ui, sans-serif; padding: .2rem .7rem; text-decoration: none; }
      .card-action:hover { border-color: #8a273c; }
      input.card-action--delete, button.card-action--delete { color: #8a273c; cursor: pointer; font: .78rem system-ui, sans-serif; }
      .grapes-table { width: 100%; border-collapse: collapse; background: #fff; border-radius: .5rem; overflow: hidden; box-shadow: 0 1px 3px rgba(0,0,0,.1); }
      .grapes-table thead { background: #f8f9fa; }
      .grapes-table th, .grapes-table td { padding: .75rem 1rem; text-align: left; border-bottom: 1px solid #dee2e6; }
      .grapes-table th { font-weight: 600; color: #495057; }
      .grapes-table tbody tr.grapes-row--even { background: #ffffff; }
      .grapes-table tbody tr.grapes-row--odd { background: #eef2f7; }
      .grapes-table tbody tr.grapes-row--even:hover,
      .grapes-table tbody tr.grapes-row--odd:hover { background: #d9e2ec; }
      .grapes-table tbody tr a { color: #6b2834; font-weight: 600; }
      .grapes-table .actions { display: flex; gap: .5rem; }
      .sort-form { display: flex; align-items: center; gap: .5rem; }
      .sort-label { font: .875rem system-ui, sans-serif; color: #495057; }
      .sort-select { padding: .375rem .75rem; border: 1px solid #dee2e6; border-radius: .25rem; font-size: .875rem; background: #fff; cursor: pointer; }
      .sort-select:focus { outline: none; border-color: #6b2834; }
      .page-actions { display: flex; align-items: center; gap: 1rem; margin-bottom: 1.5rem; }
      .producers-table__image-col { width: 72px; }
      .producers-table__thumb { display: block; width: 44px; height: 44px; object-fit: cover; border-radius: 6px; border: 1px solid #dee2e6; background: #fff; }
      .producers-table__thumb--empty { background: #eef2f7; border-style: dashed; }
    </style>
  </head>
  <body>
    <header class="site-header">
      <%= link_to root_path, class: "site-header__logo" do %>
        <img src="/wine_words.jpg" alt="Wine Words logo">
        <div>
          <div class="site-header__title">Wine Words</div>
          <p class="site-header__subtitle">Explore producers, wines and reviews</p>
        </div>
      <% end %>
      <nav class="site-nav" aria-label="Primary navigation">
        <%= link_to "Dashboard", root_path %>
        <%= link_to "Producers", producers_path %>
        <%= nav_dropdown("Wines", "All Wines", wines_path, :for_wine, :sort_order_wine) %>
        <%= nav_dropdown("Reviews", "All Reviews", reviews_path, :for_review, :sort_order_review) %>
        <%= nav_dropdown("Articles", "All Articles", articles_path, :for_article, :sort_order_article) %>
        <% if current_user&.super_admin? || current_user&.wine_manager? %>
          <details class="site-nav__settings">
            <summary>Settings</summary>
            <div class="site-nav__settings-menu">
              <% if current_user&.super_admin? %>
                <%= link_to "Users &amp; Roles".html_safe, user_roles_path %>
              <% end %>
              <%= link_to "Categories", categories_path %>
              <%= link_to "Grapes", grapes_path %>
                            <%= link_to "Countries", countries_path %>
              <%= link_to "Regions", regions_path %>
            </div>
          </details>
        <% end %>
        <% if user_signed_in? %>
          <span class="site-nav__user"><%= current_user.name.presence || current_user.email %></span>
          <%= button_to "Sign out", logout_path, method: :delete, form_class: "site-nav__signout", class: "site-nav__auth" %>
        <% else %>
          <%= link_to "Login / Sign up", login_path, class: "site-nav__auth" %>
        <% end %>
      </nav>
    </header>
    <% if notice %>
      <div class="flash flash--notice"><%= notice %></div>
    <% end %>
    <% if alert %>
      <div class="flash flash--alert"><%= alert %></div>
    <% end %>
    <main><%= yield %></main>
    <script>
      // Close any other open nav dropdown when opening/clicking another one,
      // and close dropdowns when clicking anywhere outside them.
      (function () {
        var dropdowns = Array.prototype.slice.call(
          document.querySelectorAll("details.site-nav__settings")
        );
        dropdowns.forEach(function (dropdown) {
          dropdown.addEventListener("toggle", function () {
            if (!dropdown.open) { return; }
            dropdowns.forEach(function (other) {
              if (other !== dropdown) { other.removeAttribute("open"); }
            });
          });
        });
        document.addEventListener("click", function (event) {
          dropdowns.forEach(function (dropdown) {
            if (dropdown.open && !dropdown.contains(event.target)) {
              dropdown.removeAttribute("open");
            }
          });
        });
        // Open on hover (like the React app), close shortly after the pointer
        // leaves. Click still toggles for touch/keyboard users.
        dropdowns.forEach(function (dropdown) {
          var closeTimer = null;
          dropdown.addEventListener("mouseenter", function () {
            if (closeTimer) { clearTimeout(closeTimer); closeTimer = null; }
            dropdown.setAttribute("open", "");
          });
          dropdown.addEventListener("mouseleave", function () {
            if (closeTimer) { clearTimeout(closeTimer); }
            closeTimer = setTimeout(function () {
              dropdown.removeAttribute("open");
              closeTimer = null;
            }, 250);
          });
        });
      })();
    </script>
  </body>
</html>
