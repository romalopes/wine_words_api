<%= form_with model: @wine, local: true, html: { multipart: true } do |f| %>
  <div>
    <%= f.label :producer_id %><br>
    <%= f.collection_select :producer_id, Producer.order(:name), :id, :name, include_blank: true %>
  </div>
  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :region %><br>
    <%= f.text_field :region %>
  </div>
  <div>
    <%= f.label :color %><br>
    <%= f.select :color, Wine.colors, include_blank: true %>
  </div>
  <div>
    <%= f.label :closure %><br>
    <%= f.text_field :closure %>
  </div>
  <div>
    <%= f.label :alcohol_percentage %><br>
    <%= f.number_field :alcohol_percentage, step: 0.1 %>
  </div>
  <div>
    <%= f.label :volume_ml %><br>
    <%= f.number_field :volume_ml %>
  </div>
  <div>
    <%= f.label :prompt %><br>
    <%= f.text_area :prompt %>
  </div>
  <div>
    <%= f.label :images, "Images" %>
    <%= f.file_field :images, multiple: true, direct_upload: false %>
  </div>
  <div><%= f.submit class: "btn" %></div>
<% end %>

<% if @wine.images.attached? %>
  <div class="image-gallery">
    <p class="lede">Existing images (click Remove to delete)</p>
    <% @wine.images.each do |image| %>
      <div class="image-gallery__item">
        <%= image_tag url_for(image), class: "gallery-img", alt: @wine.name %>
        <%= button_to "Remove", purge_image_wine_path(@wine.slug), method: :patch, params: { image_id: image.id }, class: "image-gallery__remove" %>
      </div>
    <% end %>
  </div>
<% end %>
